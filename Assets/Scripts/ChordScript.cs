using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChordScript : MonoBehaviour
{
    // private String ChordDirection;
    
    // Start is called before the first frame update
    void Start()
    {
        // Instantiate (ChordDirection,new Vector3(1.0f,1.0f,1.0f),Quaternion.identity);
        // Chord.Add(ChordDirection);
        // Debug.Log(Chord);
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // public List<String> Chord = new List<String>();   // リストを作る

}
